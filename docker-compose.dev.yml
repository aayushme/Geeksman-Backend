version: "3"
services: 
    node-app:
        build: 
            context: . 
            args:
                NODE_ENV: development
        ports:
            - "5000:5000"    
        volumes: 
            - ./:/app
            - /app/node_modules
        env_file: 
            - ./.env    
        environment: 
            - NODE_ENV=development 
        command:    npm run dev